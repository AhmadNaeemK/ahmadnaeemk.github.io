import { styled } from "@mui/system";

const FooterDiv = styled('div')`
    margin-top: 1rem;
    padding: 1rem;
    background-color: rgb(235, 195, 64);
    position: fixed;
    bottom: 0;
    left: 0;
    width: 100%;
`

const Footer = (props) => {
    return(
        <FooterDiv>
            
        </FooterDiv>
    )
}