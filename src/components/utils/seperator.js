import { styled } from "@mui/system";

const Seperator = styled('div')`
    width: 80%;
    height: 2px;
    margin: auto;
    background-image: radial-gradient(#CCC, rgba(0,0,0,0)), radial-gradient(#CCC, rgba(0,0,0,0));
    background-position: 80%, 0, 80%, 0;
`
export default Seperator;